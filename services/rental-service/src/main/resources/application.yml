server:
  port: ${SERVER_PORT:8084}

spring:
  application:
    name: rental-service
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/rental_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh}
    username: root
    password: Buinhatlong0912
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
